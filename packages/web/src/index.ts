// web-client — Web Client
// Scope: Browser-based SPA that renders an interactive 9x9 Sudoku board, allows difficulty selection, fetches puzzles from the Puzzle Service, submits completed boards for validation, sends results to the Scores Service, and displays the leaderboard.
// Owns: packages/web
// This team builds its slice here each phase.
export {};
