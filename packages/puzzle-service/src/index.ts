// puzzle-service — Puzzle Service
// Scope: Stateless HTTP service that generates valid, uniquely-solvable Sudoku puzzles per difficulty and validates submitted boards by re-checking Sudoku rules directly (no stored solution). No caching. No persistence is required for core functionality; any audit logging is optional and out of MVP scope unless separately requested.
// Owns: packages/puzzle-service
// This team builds its slice here each phase.
export {};
