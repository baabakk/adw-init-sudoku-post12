// scores-service — Scores Service
// Scope: HTTP service that persists completed-game results (player name, difficulty, time-to-solve) and serves a per-difficulty top-10 leaderboard. Owns a required SQLite database with WAL mode and composite index.
// Owns: packages/scores-service
// This team builds its slice here each phase.
export {};
